# Прогресс

Живой чеклист по всей задаче. Правило: пункт отмечается выполненным **после**
того, как результат проверен, а не когда работа начата. Если пункт закрыт
частично — он остаётся открытым, а недоделанное выносится подпунктом.

Приоритеты P0/P1/P2 и нумерация требований — из
[requirements.md](docs/requirements.md#4-приоритеты).

---

## Этап 0 — репозиторий, требования, дизайн

- [x] Переключить git-аккаунт на Anna Belova (`gh` + локальная identity репозитория)
- [x] Инициализировать репозиторий
- [x] Собрать требования из Figma → [requirements.md](docs/requirements.md)
- [x] Снять замеры макета → [design-spec.md](docs/design-spec.md)
- [x] Провалидировать макет, собрать расхождения → [design-validation.md](docs/design-validation.md)
- [x] Зафиксировать источники в Figma → [figma-sources.md](docs/figma-sources.md)
- [x] Спроектировать модель данных → [data-model.md](docs/data-model.md)
- [x] Инструкции для агентов → [CLAUDE.md](CLAUDE.md)
- [x] Создать private-репозиторий и запушить — [belovaaanya/voc-dashboard-b2b](https://github.com/belovaaanya/voc-dashboard-b2b)
- [x] Включить GitHub Pages — [belovaaanya.github.io/voc-dashboard-b2b](https://belovaaanya.github.io/voc-dashboard-b2b/); репозиторий переведён в public, потому что на бесплатном плане Pages для private недоступен
- [x] Убедиться, что deploy проходит зелёным и страница отдаётся по URL

## Этап 1 — данные

- [x] Выбрать стек — без framework и bundler, ES-модули + CSS custom properties ([README](README.md#стек))
- [x] Генератор тестовых данных → `data/voc-dashboard.xlsx` — детерминированный, ориентиры карточки соблюдены ([data-model §9](docs/data-model.md#9-тестовые-данные))
- [x] Скрипт проверки непротиворечивости данных — `tools/check_data.py`, каждый инвариант проверен падением в `tools/test_pipeline.py`
- [x] Конвертер `xlsx` → `site/data/*.json` — `tools/xlsx_to_json.py`
- [x] Подключить конвертер к deploy-workflow — `check_data.py` и `xlsx_to_json.py` стоят перед публикацией, deploy зелёный, `site/data/` отдаётся по URL

## Этап 2 — каркас

Реализация и проверки — [ui-shell.md](docs/ui-shell.md).

- [x] Токены из палитры макета (цвет, отступы, типографика) — контраст пар проверяется [`site/dev/contrast.html`](site/dev/contrast.html)
- [x] Сетка `1400`, исправляющая V-01…V-03 — рейл одной ширины, колонки в сумме `1400`, карточки по контейнеру
- [x] Загрузка данных и состояние фильтров как сквозной контекст — контекст блока зафиксирован в [ui-shell](docs/ui-shell.md#контракт-блока), в URL пишутся канал, семантика периода и все поддержанные фильтры
  - [x] Контролы периода и фильтров — пять preset, custom range и multi-select с `Применить` / `Сбросить`; URL, Reload, Back / Forward и keyboard flow проверены (`D-31`, `V-27`)
- [x] Пустые и ошибочные состояния (нет данных, не загрузились) — проверены принудительно: убранный файл даёт ошибку, период вне данных — «нет данных»
- [x] Фикстура в `site/fixture/` и явная пометка демо-данных — проверено с двух сторон: без `site/data/` пометка есть, с подложенной выгрузкой её нет

Проверялось на фикстуре и на выгрузке, собранной руками по контракту. Сверка с
настоящей выгрузкой конвертера — когда этап 1 её отдаст.

## Ядро расчётов

Чистые функции без DOM, тестами вперёд → [calc-core.md](docs/calc-core.md).
Идёт параллельно этапам 2 и 3, поэтому пункты свои.

- [x] Периоды: preset → диапазон, «предыдущий аналогичный», выравнивание неполного периода (`D-23`, `D-24`)
- [x] Базовые агрегации: VOC по оценкам, количество, низкие, распределение, группировка (`D-07`)
- [x] Неаддитивные разрезы: вес `1/N`, упоминания, дедупликация по `voc_ccode` (`D-08`, `D-09`)
- [x] Группа «Без разметки» и гарантия, что оценка не теряется ни при какой проекции (`D-11`)
- [x] Вклад антидрайвера: разложение `ΔVOC` на эффект оценки и эффект структуры (`D-10`)
- [x] Достаточность выборки: полуширина 95 % CI, настраиваемый порог (`D-25`)
- [x] Округление на выводе: VOC 2, дельта 2, вклад 3, доли ровно 100 % (`D-21`)
- [x] Инварианты проверены с двух сторон — positive control на каждый (`test/negative-controls.test.js`)
- [x] Независимый review ядра и правка находок: `years-5`, `voc_ccode`, разбор дат, порядок групп, `t`-квантиль, два беззубых guard'а

## Блоки метрик

Первый блок на реальных данных и контракт, в который встают следующие →
[blocks-metrics](docs/blocks-metrics.md), [контракт](docs/ui-shell.md#контракт-блока).

- [x] Контракт блока: реестр `id → renderer`, единый объект контекста, `blocks/<id>.js` — зафиксирован в `ui-shell.md`, проверяется `test/blocks-contract.test.js`, каждый guard сломан на заведомо нарушающем исходнике
- [x] Большая карточка VOC канала: значение, объём, дельта, коридор, бейдж — три исхода бейджа и срез без плана проверены на выгрузке
- [x] Три сегментные карточки: значение, дельта, объём; сверены с независимым пересчётом
- [x] Вторая строка метрик: `Оценок`, `Низких оценок`, `Доля 5★` (`D-15`)
- [x] Кликабельность сегментных карточек: фильтр в URL, обход по `Tab`, `aria-pressed`, чип состояния; проверено в grayscale
- [x] Пометка недостаточной выборки на карточках (`D-25`) — видна на `КИБ`, отсутствует на `ММБ`
- [x] Служебные состояния проверены принудительно: период без данных, убранный `site/data/`

## Этап 3 — P0

- [x] №1 Период как главный элемент интерфейса — первый выделенный контрол шапки, диапазон совпадает со срезом (`V-11`)
- [x] №2 Архитектура глобального + локального периода — global period в URL, local period через единый `context.select`, UI локального выбора остаётся слайсу конкретного блока
- [x] №3 Сравнение с предыдущим аналогичным периодом
  - [x] в карточках метрик — дельта к предыдущему аналогичному периоду (`D-23`, `D-24`)
  - [x] на графике — пунктирная линия по effective previous period (`D-23`, `D-24`, `V-11`)
  - [x] в антидрайверах — рейтинг вклада по effective previous period, честное состояние без сравнения
- [x] №4 Количество оценок на графике VOC — столбцы и общий объём с переключателем (`V-28`)
- [x] №5 Антидрайверы: рейтинг «кто главный» + «как изменился», sparkline и heatmap второго уровня (`D-10`, `D-21`, `D-27`, `V-29`)
- [x] №6 Связка график → антидрайверы → события — дата в `focus`, выбор с графика и инсайта, однодневный local period антидрайверов (`D-31`, `V-30`)
- [x] №7 Интерактивные карточки метрик — клик по сегменту ставит фильтр, остальные блоки перестраиваются
- [ ] №8 Распределение оценок 1–5

## Этап 4 — P1

- [ ] №9 Sparkline в карточках метрик
- [x] №10 Инсайты вместо календаря событий — дневные изменения VOC и причина из вклада проблем (`D-16`, `V-17`, `V-30`)
- [x] №11 Перестройка инсайтов при фильтрации — список и причины считаются из текущего `context.slice`
- [ ] №12 Индикация недостаточной выборки
  - [x] на карточках метрик (`D-25`)
  - [ ] на срезах фильтров — отдельный слайс после подписи аналитики для `D-25`
- [ ] №13 Раскрытие детализации антидрайверов
- [x] №14 Быстрые стандартные периоды — месяц, полгода, календарный год, 365 дней и 5 лет от `manifest.period.to` (`D-41`)

## Этап 5 — P2

- [ ] №15 Облако проблем из прямой речи
- [ ] №16 Более сложные инсайты / выводы LLM
- [ ] №17 Продвинутая тепловая карта
- [ ] №18 Чат с вопросами по данным

## Блоки вне списка приоритетов

Отрисованы в расширенном варианте макета, но в P0/P1/P2 не попали — приоритет
надо назначить. Подробно в [requirements 3.8](docs/requirements.md#38-прямая-речь)
и [3.9](docs/requirements.md#39-аномалии).

- [ ] Вторая строка метрик: `Стабильность`, `Конверсия в опрос`, `Покрытие` — сама строка есть, но заполнена считаемым из выгрузки (`D-15`); этим трём нужен источник
- [ ] Прямая речь: карточки частотных проблем (табы «Частотные» / «Новые»)
- [ ] Прямая речь: таблица детализации с поиском
- [x] Аномалии — не отдельный блок: будущий тип в общем списке инсайтов (`D-16`)

---

## Пересмотреть с заказчиком

Ничего из этого не блокирует реализацию: все 17 открытых вопросов закрыты
решениями по умолчанию в [decisions.md](docs/decisions.md). Здесь — то, что
стоит пересмотреть, когда заказчик дойдёт.

Меняет смысл цифр, а не код:

- [ ] `D-10` Методика вклада антидрайвера — формула даёт проверяемый результат, но «правильность» определяет аналитика
- [ ] `D-25` Порог достаточности выборки — значение по умолчанию заведомо заглушка
- [ ] `D-05` Природа `OSLK_EXPERTISE_NAME` — если это клиентский текст, дашборд теряет часть прямой речи
- [ ] `V-23` Колонка `УИУКО` в таблице детализации макета — её нет ни в одном перечне, и содержимое похоже на внутреннюю оценку, а не на прямую речь. Связано с `D-05`
- [ ] `D-25` Область применения порога: карточки и срезы — да, ячейки тепловой карты — нет (иначе помечена почти половина карты)

Остальное — при появлении источника или макета:

- [ ] `D-03` Плановый коридор: подтвердить, что план ведётся отдельным листом
- [ ] `D-42` Коридор снимается любым фильтром, кроме сегмента, и запись обязана покрывать период целиком — проверить, что это совпадает с тем, как план ведёт аналитика
- [ ] `D-43` Сегментные карточки не сужаются собственным фильтром — проверить на живом сценарии PO
- [ ] `D-13` Фильтры `Роли` и `ФО` убраны — вернуть, когда появятся колонки
- [ ] `D-15` `Стабильность`, `Конверсия в опрос`, `Покрытие` — нужен источник
- [ ] `D-17` `ФО`, `Платформа`, `X-pin`, `U-pin` в таблице детализации — нужен источник
- [ ] `D-29` Адаптив — нужен макет планшета или мобильного
- [ ] `D-30` Гарнитура — нужна брендовая с лицензией
- [ ] `V-16` `Лояльность` — драйвер роста или антидрайвер
- [ ] Приоритет блоков вне списка P0–P2 (см. раздел выше)
- [ ] Финальная Figma-валидация всех реализованных блоков: повторно сверить композицию, размеры, типографику, состояния и интерактивность; зафиксировать новые расхождения как `V-xx` и исправить уже сделанные блоки, которые сейчас визуально не соответствуют дизайну
