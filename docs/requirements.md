# Требования — дашборд VOC для PO B2B

Источник требований — текстовая спецификация и макеты в Figma-файле
«[D] дашборд VOC для PO B2B». Ссылки и node ID собраны в [figma-sources.md](figma-sources.md).

Документ описывает **что** должен делать дашборд. Замеры вёрстки лежат в
[design-spec.md](design-spec.md), расхождения в референсном макете — в
[design-validation.md](design-validation.md).

---

## 1. Контекст

Дашборд показывает VOC (Voice of Customer) по каналам B2B и служит инструментом
product owner'а: не просто витрина метрик, а **система исследования причин** —
почему VOC вырос или упал.

Ключевая формулировка из спецификации:

> Дашборд постепенно превращается из набора аналитических блоков в систему
> исследования причин.

Верхнеуровневый переключатель канала: `АБМ` / `АБ-WEB`.

Форм-фактор — обычная скроллящаяся веб-страница, блоки дашборда идут потоком
внутри неё; макет задаёт горизонтальную структуру, но не фиксированную высоту
экрана ([design-spec](design-spec.md#страница-а-не-артборд)).

### Словарь

Термины взяты из макета как есть. Расшифровки не зафиксированы в источнике и
требуют подтверждения у заказчика:

| Термин | Где встречается |
| --- | --- |
| `АБМ`, `АБ-WEB` | переключатель канала в шапке |
| `ММБ`, `СБ`, `КИБ` | сегменты в карточках метрик |
| `КП`, `ФО` | фильтры в шапке |
| Антидрайверы: `Платежи`, `Выписка`, `Лояльность`, `АЗОН`, `Реестры`, `Зарплатный проект` | heatmap / рейтинг антидрайверов |
| `CX-календарь` | источник событий для инсайтов |

---

## 2. Сквозные принципы

### 2.1. Период — сквозной контекст, а не фильтр

Любая аналитика VOC начинается с вопроса «за какой период смотрим данные»,
поэтому период должен быть одним из главных элементов интерфейса и заметно
выделяться в шапке.

**Архитектура периода — гибридная:**

- глобальный период задаётся сверху и действует на весь дашборд;
- отдельные блоки могут менять период/масштаб локально;
- всегда должно быть очевидно, какой период применён к конкретному блоку;
- нужна механика «сбросить всё к общему периоду».

**Быстрые предустановки периода:** месяц, полгода, текущий календарный год,
365 дней, 5 лет; список открыт для дополнения.

**Сравнение с предыдущим аналогичным периодом:** месяц к месяцу, квартал к
квакталу, неделя к неделе. Сравнение включается отдельным действием
«Сравнить», а не показывается постоянно.

### 2.2. Интерактивность: проваливаться в контекст

Элементы должны не просто показывать данные, а позволять проваливаться в
контекст. Клик по любому смысловому элементу меняет контекст всего дашборда:

| Элемент | Клик | Результат |
| --- | --- | --- |
| Карточка `VOC СБ` | → | фильтр СБ; перестраиваются график, антидрайверы, инсайты, прямая речь |
| Антидрайвер «Выписка» | → | детализация по выписке |
| Дата на графике | → | события + антидрайверы + проблематики этого дня |

Кликабельность карточки должна быть понятна визуально.

---

## 3. Требования по блокам

### 3.1. Верхние ключевые метрики

Текущий состав: VOC канала, VOC ММБ, VOC СБ, VOC КИБ, стабильность,
конверсия в опрос, покрытие.

В референсном wireframe видны только четыре VOC-карточки. В расширенном
варианте макета все семь показателей разложены в две строки: VOC-карточки
сверху, `Стабильность` / `Конверсия в опрос` / `Покрытие` — второй строкой
([`variant-voc-2-0.png`](design/reference/variant-voc-2-0.png), см.
[`V-10a`](design-validation.md#v-10a--скрытая-вторая-строка-метрик--это-контент)).

**Правки:**

- **Sparkline в каждом виджете метрики** — и в большом виджете VOC канала, и в
  маленьких карточках. Цель: мгновенно считывать направление и характер
  динамики, не открывая отдельный график. Композиция карточки:
  значение → sparkline → дельта (`4.90` → ~~~ → `+0,04`).
- **Карточки кликабельны** — клик по сегменту применяет соответствующий фильтр
  либо раскрывает детализацию; остальные блоки перестраиваются под выбранный
  контекст.
- **Пересмотреть состав метрик.** «Конверсия в опрос» не является значимой
  метрикой для большинства пользователей. Кандидаты на добавление:
  - количество оценок;
  - количество низких оценок;
  - распределение оценок 1 / 2 / 3 / 4 / 5;
  - возможно, отдельный показатель по низким оценкам для КИП/СБ.

### 3.2. Динамика VOC

Один из ключевых блоков для переработки.

- **Обязательно добавить количество оценок.** Сейчас график показывает VOC, но
  не показывает объём выборки. Обоснование: VOC вырос, а количество оценок
  упало в 3 раза → рост может быть статистически незначимым. Частотность
  оценок — обязательный контекст для интерпретации VOC, а не второстепенная
  метрика.
- **Наложение дополнительных показателей:** включить/выключить частотность
  оценок; выбрать дополнительную метрику; посмотреть влияние или динамику
  конкретного продукта либо другого измерения.
- **Связь с антидрайверами.** Если в конкретный день произошла просадка VOC,
  пользователь должен быстро понять, за счёт чего:
  `21.05 → VOC ↓ → платежи / выписка / лояльность ↓`.

Сравнение периодов на графике в расширенном варианте уже отрисовано: третья
серия легенды «Прошлый период», пунктирная линия поверх текущей. Это готовый
дизайн для P0 №3.

### 3.3. Антидрайверы

Самая существенная содержательная правка. Текущий heatmap с большим
количеством цифр и зелёно-красных ячеек сложнее, чем задача блока.

**Блок должен отвечать на два вопроса:**

1. Кто главный антидрайвер в текущем периоде?
2. Как он изменился относительно предыдущего аналогичного периода?

Примеры целевой формулировки:

> **Выписка** — главный антидрайвер, −0,12 VOC к предыдущему месяцу

> **Платежи** — главный антидрайвер → было №3 → стало №1

**Целевая структура блока:**

- на первом уровне — компактный рейтинг антидрайверов:

  | # | Антидрайвер | Влияние |
  | --- | --- | --- |
  | 1 | Выписка | −0,12 |
  | 2 | Платежи | −0,08 |
  | 3 | Лояльность | −0,05 |
  | 4 | Зарплатный проект | −0,03 |

- по раскрытию — детализация влияния, динамика, дополнительные разрезы,
  тепловая карта.

То есть: **сначала «что происходит?» → по клику «почему происходит?»**

Дизайн этого рейтинга существует — блок «Топ проблем» в расширенном варианте:
номер, проблема, поясняющая строка («Сбой при экспорте выписки»), sparkline
тренда и дельта. Верстать рейтинг нужно по нему, а не изобретать заново.

Тепловую карту можно сохранить, но не как главный способ чтения блока — она
становится вторым уровнем детализации.

### 3.4. Единая история: график → антидрайверы → события

Сейчас календарь событий воспринимается как отдельный блок, поэтому его связь
с графиком не считывается. Нужно визуально показать, что
**график + антидрайверы + события = единая история объяснения динамики**:

```
Динамика VOC
  ↓
сигналы / антидрайверы под конкретными датами
  ↓
события / релизы / сбои
```

Пример: `21.05 — VOC ↓` → 🔴 Платежи, 🔴 Выписка, 🔴 Лояльность →
CX-календарь: сбой / релиз / изменение сценария.

### 3.5. «Календарь событий» → «Инсайты»

Не переименование, а изменение концепции блока.

**Первый этап** — инсайты формируются сравнительно просто: сопоставление с
CX-календарём, рост/падение метрик, аномальные изменения, простые выводы LLM.
Примеры:

> Оценки по зарплатному проекту снизились в 3 раза относительно предыдущего периода.

> Падение VOC связано с ростом негативных оценок по выписке.

**Далее** — блок автоматически перестраивается при изменении периода,
сегмента, региона и других фильтров: выбрали «Средний бизнес» → инсайты
пересчитались для СБ; выбрали Санкт-Петербург → пересчитались для
Санкт-Петербурга.

**Сортировка инсайтов** — два режима: по влиянию на VOC и хронологически.
Вопрос вторичный по отношению к самой механике инсайтов.

### 3.6. Индикация недостаточного объёма данных

Отдельная аналитическая задача. Если за выбранный период мало оценок,
пользователь должен понимать, что вывод может быть ненадёжным
(например: «Реестры — 50 оценок за месяц» + сигнал «⚠ недостаточный объём
выборки»).

**Порог нельзя выбирать произвольно** — он определяется
статистически/методологически. Порядок работы строгий:

1. определить критерий достаточности выборки;
2. посчитать пороги;
3. только потом проектировать визуальный индикатор.

**Где показывать** — обсуждались варианты: прямо в блоке, специальный значок,
сообщение в инсайтах («Количество оценок за выбранный период недостаточно для
надёжного вывода»). Последний вариант предпочтителен: это интерпретация
данных, а не техническое предупреждение.

### 3.7. Распределение оценок

Простая диаграмма распределения:

| Оценка | Доля |
| --- | --- |
| 1 ★ | 5 % |
| 2 ★ | 7 % |
| 3 ★ | 10 % |
| 4 ★ | 25 % |
| 5 ★ | 53 % |

Варианты размещения: отдельный верхний виджет, дополнительный вид внутри
верхнего блока, фильтруемый элемент. Связана с фильтрами: выбрали СБ →
распределение перестроилось по СБ.

### 3.8. Прямая речь

В текстовой спецификации прямая речь упомянута только как источник облака
тегов. В расширенном варианте макета под неё отрисованы **два полноценных
блока**, которых нет ни в wireframe, ни в разделах спецификации.

#### Карточки частотных проблем

Горизонтальный ряд карточек с табами `Частотные` / `Новые`. В карточке:

- тег проблемы (`Стабильность`, `Лояльность`, `Рублевые платежи`, `Подписание`);
- доля от ОС в процентах и направление тренда;
- цитата клиента дословно («Платёж отклоняется без объяснений»);
- sparkline тренда;
- влияние на VOC с точностью до тысячных (`−0,044`).

Блок помечен «made by AI».

#### Таблица детализации

Отдельный блок «Прямая речь: детализация» — таблица сырых комментариев с
поиском. Колонки в макете: `Дата`, `Сегмент`, `Сценарий`, `ФО`, `Платформа`,
`X-pin`, `U-pin`, `Комментарий`, `Категория`.

**Требует подтверждения:** назначение колонок `X-pin` / `U-pin` и полный
перечень колонок — по макету однозначно не читаются.

#### Облако проблем и тегов

Автоматически формируемое облако наиболее частотных проблем и слов, которое
перестраивается при смене периода, фильтрации по сегменту, выборе продукта и
других фильтрах.

Источник — **размеченная** прямая речь клиента, а не все слова из
комментариев. Потенциальный конвейер:
`прямая речь → очистка/нормализация → выделение проблем → облако`.

Статус: концепт (P2), не обязательная правка текущей версии — в отличие от двух
блоков выше, у которых дизайн уже есть.

### 3.9. Аномалии

В расширенном варианте — отдельный блок, не тождественный «Инсайтам»: строки с
датой, тегом (`Рост негатива`, `Спад упоминаний`), дельтой и отметкой
`негатив`. То есть аномалии по объёму и тональности упоминаний, тогда как
инсайты объясняют динамику VOC.

**Требует подтверждения:** остаются ли «Аномалии» и «Инсайты» двумя блоками
или сливаются в один. Спецификация говорит только об инсайтах, а макет
показывает оба.

---

## 4. Приоритеты

Приоритизация зафиксирована в макете отдельным блоком.

### P0 — обязательно переработать

| # | Требование | Раздел |
| --- | --- | --- |
| 1 | Период сделать главным элементом интерфейса | [2.1](#21-период--сквозной-контекст-а-не-фильтр) |
| 2 | Определить архитектуру глобального + локального периода | [2.1](#21-период--сквозной-контекст-а-не-фильтр) |
| 3 | Добавить сравнение с предыдущим аналогичным периодом | [2.1](#21-период--сквозной-контекст-а-не-фильтр) |
| 4 | Добавить количество оценок на график VOC | [3.2](#32-динамика-voc) |
| 5 | Переделать антидрайверы под вопросы «кто главный сейчас» и «как изменился» | [3.3](#33-антидрайверы) |
| 6 | Связать график → антидрайверы → события в единую историю | [3.4](#34-единая-история-график--антидрайверы--события) |
| 7 | Сделать карточки/метрики интерактивными | [3.1](#31-верхние-ключевые-метрики) |
| 8 | Добавить/проработать распределение оценок 1–5 | [3.7](#37-распределение-оценок) |

### P1 — следующий слой

| # | Требование | Раздел |
| --- | --- | --- |
| 9 | Sparkline в карточках метрик | [3.1](#31-верхние-ключевые-метрики) |
| 10 | Автоматические инсайты вместо простого календаря событий | [3.5](#35-календарь-событий--инсайты) |
| 11 | Динамическая перестройка инсайтов при фильтрации | [3.5](#35-календарь-событий--инсайты) |
| 12 | Индикация недостаточной выборки | [3.6](#36-индикация-недостаточного-объёма-данных) |
| 13 | Возможность раскрывать детализацию антидрайверов | [3.3](#33-антидрайверы) |
| 14 | Быстрые стандартные периоды | [2.1](#21-период--сквозной-контекст-а-не-фильтр) |

### P2 — развитие

| # | Требование | Раздел |
| --- | --- | --- |
| 15 | Облако проблем/тегов из прямой речи | [3.8](#38-прямая-речь) |
| 16 | Автоматическое формирование более сложных инсайтов/выводов LLM | [3.5](#35-календарь-событий--инсайты) |
| 17 | Более продвинутая тепловая карта | [3.3](#33-антидрайверы) |
| 18 | Чат с возможностью задавать вопросы по данным | — |

---

## 5. Открытые вопросы

Требуют решения заказчика — до реализации соответствующих пунктов.

| # | Вопрос | Блокирует |
| --- | --- | --- |
| 1 | Расшифровка и полный список сегментов, каналов, фильтров (`КП`, `ФО`, `АЗОН` и др.) | подписи и структуру фильтров |
| 2 | Критерий достаточности выборки и конкретные пороги | P1 №12 |
| 3 | Методика расчёта вклада антидрайвера в VOC (`impact_voc`) | P0 №5, рейтинг антидрайверов |
| 4 | Нужен ли адаптив под планшет/мобильный — в макете только desktop 1600 | вёрстку сетки |
| 5 | Состав метрик после отказа от «конверсии в опрос» | P0 №7, верхний блок |
| 6 | Назначение колонок `X-pin` / `U-pin` и полный перечень колонок таблицы детализации | 3.8, лист `speech_verbatim` |
| 7 | «Аномалии» и «Инсайты» — один блок или два | 3.9 |
| 8 | Node ID расширенного варианта макета (нужна ссылка на фрейм) | вёрстку блоков вне wireframe |

Стек зафиксирован — без framework и bundler, обоснование в
[README](../README.md#стек).

## 6. Данные

Источник данных — файлы, лежащие рядом с сайтом; backend отсутствует.
Source of truth — `xlsx`, который правит аналитик, JSON для браузера
генерируется при deploy. Схема листов, состав полей и требования к тестовым
данным: [data-model.md](data-model.md).

---

## 7. Сквозные вопросы реализации

Не относятся к отдельному блоку, но всплывут в первом же слайсе, поэтому
решаются до, а не по ходу.

| # | Вопрос | Почему это не мелочь |
| --- | --- | --- |
| 1 | Доступность цветовой пары зелёный ↔ малиновый | Дейтеранопия — самый частый тип цветовой слепоты, и именно эта пара для неё неразличима. В heatmap значение дублировано цифрой, а вот у трендов, тегов и точек графика цвет — единственный носитель смысла. В макете есть проработка шкалы с промежуточным янтарным тоном ([`heatmap-scale-variants.png`](design/reference/heatmap-scale-variants.png)) — она частично решает вопрос |
| 2 | Контраст служебного текста | Подписи осей `#A4A4A5` на фоне `#0B0B0D` — надо проверить по WCAG, светлые тонкие начертания на почти чёрном читаются хуже, чем кажется в макете |
| 3 | Формат чисел и дат | Запятая как десятичный разделитель, неразрывный пробел в `10 459`, даты `dd.mm.yy`. Нужна одна функция форматирования, иначе разъедется между блоками |
| 4 | Округление | VOC до сотых, дельта до сотых, влияние на VOC до тысячных. Округление напрямую решает, попадает ли значение в плановый коридор, то есть влияет на бейдж |
| 5 | Семантика периода | «Месяц» — календарный или последние 30 дней? Что такое «предыдущий аналогичный» для неполного месяца? От этого зависят все сравнения |
| 6 | Гарнитура | В макете text styles не привязаны, шрифт не установлен. Нужен выбор и лицензия, пригодная для публичного хостинга |
| 7 | Поведение `развернуть` и `меню` в шапках карточек | Иконки нарисованы во всех блоках, поведение не описано нигде |
| 8 | Правило маркировки «made by AI» | В макете стоит на выводах, инсайтах, топе проблем и прямой речи. Нужно правило, что именно помечается |
| 9 | Состояние в URL | Период и фильтры в query-параметрах, иначе вид нельзя переслать коллеге — для PO-инструмента это базовый сценарий |
| 10 | Дата актуальности данных | Данные статичны и обновляются вручную; без явной даты выгрузки пользователь не знает, насколько свежее то, что он смотрит |
| 11 | Кэширование JSON на Pages | Обновили `xlsx` — браузер обязан увидеть новый JSON, а не отдать закэшированный. Нужен cache-busting |
| 12 | Пустые и ошибочные состояния | Нет данных за период, данные не загрузились, выборка слишком мала — по каждому нужно поведение |
